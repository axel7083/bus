
// When the map is displayed with the sidepanel, we
// need to store somewhere, if we are displaying the list of layers, or editing one.
enum ConfigurationState {
    DISPLAY,
    EDITING,
}

export default ConfigurationState;